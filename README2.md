## Этап 2: Конфигурация

### Параметры командной строки:
```bash
--vfs-path PATH      Путь к физическому расположению VFS
--log-file PATH      Путь к лог-файлу (формат JSON)
--start-script PATH  Путь к стартовому скрипту
--config PATH        Путь к конфигурационному файлу YAML
